<h1><%= @trend_post.title %></h1>

  <%= form_for @trend_post, :url =>admin_trend_post_path(@trend_post), :method => :PUT do |f| %>
    <%= render :partial => "trendform", :locals => {:form => f} %>
  <% end %>

    <%= link_to "Back", admin_trend_posts_path %>
